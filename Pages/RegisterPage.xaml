<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuizAppFront.Pages.RegisterPage"
             Title="RegisterPage">

    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="15">

            <Label Text="Nome" />
            <Entry x:Name="EntName" Placeholder="Digite seu nome" />

            <Label Text="Email" />
            <Entry x:Name="EntEmail" Placeholder="Digite seu email" Keyboard="Email" />

            <Label Text="Senha" />
            <Entry x:Name="EntPassword" Placeholder="Digite sua senha" IsPassword="True" />

            <Button Text="Registrar" Clicked="OnRegisterClicked" />

            <Label Text="Já possui uma conta?" />
            <Button  Text="Faça login"
                     Clicked="OnGoForLoginClicked"
                     TextColor="Blue"
                     HorizontalOptions="Start"
                     BackgroundColor="Transparent"
                     FontAttributes="Bold" />


        </VerticalStackLayout>
    </ScrollView>
</ContentPage>